name: Build and Cache bili_ticket_gt_python

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Cache pip
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install bili_ticket_gt_python~=0.2.7

    - name: Save compiled package
      run: |
        mkdir -p compiled_packages
        cp -r ~/.cache/pip compiled_packages/
      # You can add steps to upload the compiled packages to a storage service if needed
